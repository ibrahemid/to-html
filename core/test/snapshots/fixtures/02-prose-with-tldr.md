**TL;DR:** Short summary line covering the key point of the reply for readers in a hurry.

The first paragraph runs long enough to clear the prose threshold for the auto-render gate. It walks through context, restates the problem, and motivates the approach. Some sentences are short. Others are longer, with subordinate clauses that add detail without burying the lead. The intent is to look like a normal substantive reply that a careful assistant might write.

The second paragraph extends the prose. It introduces a couple of concrete details. It avoids markdown chrome like headings, lists, or code fences, so the classifier routes the reply to the prose template rather than to explainer or plan. The classifier weighs total length, heading count, and structural signals; this fixture leans on length alone to land cleanly in prose.
