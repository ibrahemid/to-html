{
  "name": "to-html-monorepo",
  "version": "2.1.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "core",
    "cli",
    "shared/transcript",
    "adapters/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "lint": "eslint .",
    "sync": "node scripts/sync-core.js && node scripts/sync-version.js",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "eslint": "^9.39.4"
  }
}
