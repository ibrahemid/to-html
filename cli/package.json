{
  "name": "@ibrahemid/to-html",
  "version": "2.1.0",
  "private": true,
  "description": "Universal CLI for to-html (scaffold; Phase 3).",
  "bin": { "to-html": "bin/to-html.js" },
  "engines": { "node": ">=18" },
  "scripts": { "test": "node --test 'test/**/*.test.js' || true" }
}
